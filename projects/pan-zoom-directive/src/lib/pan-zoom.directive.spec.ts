import { PanZoomDirective } from './pan-zoom.directive';

describe('PanZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new PanZoomDirective();
    expect(directive).toBeTruthy();
  });
});
