# ngld-icon

An angular svg component that allow using icons in svg elements, keeping the setting experience similar to `@angular/material/icon`.

Although this library is initially designed to work with `ngld-canvas` in `@ngld/canvas`, this component works as a general svg element for all Angular projects.
This library can be used independenly outside of `@ngld` or linked-data contexts.

## License

MIT
